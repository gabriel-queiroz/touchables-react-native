import React from 'react';

import { Touchable, Content, Title } from './styles';

const TouchableOpacity = () => (
  <Touchable>
    <Content>
      <Title>TouchableOpacity</Title>
    </Content>
  </Touchable>
);

export default TouchableOpacity;
